[project]
name = "snow-dbt-arch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sanketkuanar", email = "sanketkuanar@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.12.3",
    "dbt-snowflake>=1.12.0",
]

[project.scripts]
snow-dbt-arch = "snow_dbt_arch:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
